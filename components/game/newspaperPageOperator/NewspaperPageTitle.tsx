import React from 'react';
import { View } from 'react-native';

const NewspaperPageTitle = () => {
    return (
        <View className="flex-1 items-center justify-center min-w-[200px]">
        </View>
    );
};

export default NewspaperPageTitle;
